# Pinned-range runtime requirements for the FREE WILL simulation engine.
# Prefer `pip install -e .` from this directory (see pyproject.toml) for development;
# this file is here for environments (e.g. the Compute Engine run image, PRD Section 6.0)
# that expect a flat requirements.txt.
numpy>=1.26
scipy>=1.12
sparse>=0.15
pandas>=2.2
pyarrow>=15.0
google-cloud-storage>=2.16
google-cloud-sql-connector>=1.7
google-cloud-logging>=3.10
redis>=5.0
psycopg[binary]>=3.1
pydantic>=2.7
pyyaml>=6.0
